---
'@ankhorage/zora': major
---

Eliminate the legacy `src/patterns` ownership root. Move retained capabilities under canonical `src/features` owners, replace `SwitchField` with the Surface-aligned `Switch` API, and remove redundant convenience APIs including `ConfirmDialog`, `FilterBar`, `InspectorField`, `Notice`, `Panel`, and `ThemeComposer`.
