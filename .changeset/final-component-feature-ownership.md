---
"@ankhorage/zora": major
---

Finish ZORA feature ownership by replacing `Modal` with `Dialog`, `SearchBar` with `SearchInput`, removing `ToolbarAction`, rebuilding Tabs on the released Surface tab primitives, and migrating Pagination, Rating, and Toolbar out of `src/components`.
