---
'@ankhorage/zora': major
---

Adopt `@ankhorage/surface` 5, replace `Menu` and `DropdownMenu` with `PopoverMenu`, make AppBar overflow a real action menu, migrate Breadcrumbs and Select to feature ownership and direct manifest nodes, and present Select with Popover on web and BottomSheetFlatList on native without `@react-native-picker/picker`.
