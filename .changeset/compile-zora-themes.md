---
'@ankhorage/zora': major
---

Add a pure `@ankhorage/zora/theme` compiler that returns canonical Color Theory evidence, resolved Surface semantics, measurable selection results, diagnostics, and provenance for both theme modes. Replace the previously unconstructed computed-theme type shape with the compiler's real result.
