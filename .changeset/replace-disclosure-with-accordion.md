---
'@ankhorage/zora': major
---

Replace the legacy `DisclosureSection` pattern with feature-owned `Accordion` and `AccordionItem` manifest components backed by the published Surface accordion foundation.

BREAKING CHANGE: `DisclosureSection` and `DisclosureSectionProps` are no longer exported. Use `Accordion` with one or more `AccordionItem` children instead.
